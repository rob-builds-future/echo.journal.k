package com.example.echojournal.util

// Enum to differentiate settings navigation
enum class SettingType {
    Username,
    TargetLanguage,
    ProfileInfo,
    Theme,
    Templates,
    Reminders
}
